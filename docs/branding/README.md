# Current branding

The user supplied `~/Downloads/logo_tap2work_circle.png` on 2026-09-24. `../../tap2work.png` is the 512 px app copy; `scripts/sync-branding.mjs` copies it into Flutter assets. The whole circle is shown without the previous crop.

Web favicon (64 px), PWA icons (192/512 px), and Android/iOS launcher assets were resized from the same supplied artwork at their declared dimensions. The original remains in Downloads. Superseded logo files were removed from the working tree; Git preserves their history. Native builds and launcher rendering on devices are not verified.

Menu artwork in `app/assets/menu/menu_tap2.png` is unchanged. Manual media remains externally linked and does not enlarge the bundled assets.
